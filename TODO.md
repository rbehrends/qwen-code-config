# TODO

- Move provider catalog fetching to a non-blocking client or background task so model fetches do not tie up the UI thread.
- Consider how the UI communicates that config changes require explicit save, while custom provider/profile changes persist automatically.
